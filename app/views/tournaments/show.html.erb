  <div class='container'
    <h1><%= @tournament.theme %></h1>
  <div class='row'>
    <div class='col-lg-12'>
      <% i = 0 %>
      <% @tournament.rounds.each do |round| %>
        <p> <%= round.name %> </p>
        <% round.matches.each do |match| %>
          <% if i == 0 %>
            <%= "#{match.contestant_1.pet.name} : #{match.contestant_1.vote_count}" %>
            <br/>
          <% else %>
            <%= "#{match.contestant_1.pet.name} : #{match.contestant_1.vote_count} VS #{match.contestant_2.pet.name} : #{match.contestant_2.vote_count}" %>
            <br/>
          <% end %>
        <% end %>
        <% i += 1 %>
        <br/>
      <% end %>
      <h5>   <%= link_to 'Back to All Tournaments', tournaments_path, class: 'btn btn-primary' %> </h5>
    </div>
  </div>
  <% if current_user.id == 1 %>
    <div class='well'>
      <%= link_to 'Edit', edit_tournament_path(@tournament), class: 'btn btn-primary' %>
      <% if @tournament.rounds.count == 4 %>
        <%= link_to 'view winner', "/tournaments/#{@tournament.id}/winner", class: 'btn btn-primary' %>
      <% else %>
        <%= link_to 'Seed Next Round', "/tournaments/#{@tournament.id}/set_next_round", method: :post, class: 'btn btn-primary'  %>
      <% end %>
    </div>
  <% end %>
</div
